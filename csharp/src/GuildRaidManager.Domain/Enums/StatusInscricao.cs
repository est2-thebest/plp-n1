namespace GuildRaidManager.Domain.Enums;

/// <summary>No JSON vira minúsculo: confirmado | lista_espera | cancelado (underscore, não camelCase).</summary>
public enum StatusInscricao
{
    Confirmado,
    ListaEspera,
    Cancelado
}
