name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: csharp/global.json
      - name: Restore
        run: dotnet restore csharp/GuildRaidManager.sln
      - name: Build
        run: dotnet build csharp/GuildRaidManager.sln --no-restore --configuration Release
      - name: Test
        run: dotnet test csharp/GuildRaidManager.sln --no-build --configuration Release
